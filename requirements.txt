# This file lists the dependencies for your Streamlit application.
# It ensures that all required libraries are installed when the app is deployed.

# Core dependencies based on the visible list in your screenshot
altair==4.2.0
matplotlib==3.6.2
numpy==1.23.2
packaging==21.3
pandas==1.5.1
Pillow==9.3.0
protobuf==3.19.6
pyarrow==9.0.0
pydeck==0.8.0
python-dateutil==2.8.2
pytz==2022.6
referencing==0.30.2
requests==2.28.1
streamlit==1.15.2
tenacity==8.1.0
toml==0.10.2
tornado==6.2
typing-extensions==4.4.0
tzdata==2022.6
urllib3==1.26.12
watchdog==2.1.9

# The dependency that caused the error
joblib==1.2.0

# Other potentially useful libraries that are common in data apps
# You might need to add or remove these based on your actual code
scikit-learn==1.1.3
plotly==5.11.0
# This file lists the dependencies for your Streamlit application.
# It ensures that all required libraries are installed when the app is deployed.

# Core dependencies (as per previous analysis and common usage)
streamlit
pandas
numpy
altair
Pillow
protobuf
pyarrow
pydeck
matplotlib
joblib

# The dependency causing the current error
scikit-learn

# Other potentially useful libraries that are common in data apps
# You might need to add or remove these based on your actual code
plotly
